# =============================================================================
# Vectorless Semantic Search System — Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
#
# For GPU support, install PyTorch with CUDA first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then install the rest:
#   pip install -r requirements.txt
# =============================================================================

# --- Core ML / NLP ---
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0
tokenizers>=0.15.0

# --- Search Backends ---
elasticsearch>=8.10.0
hnswlib>=0.7.0
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4            # Uncomment for GPU FAISS (requires CUDA toolkit); remove faiss-cpu first

# --- PostgreSQL ---
psycopg2-binary>=2.9.9

# --- Graph ---
networkx>=3.2

# --- RAG / LLM ---
openai>=1.6.0

# --- Data / Evaluation ---
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.1.0
scikit-learn>=1.3.0
pytrec-eval-terrier>=0.5.6

# --- API (optional — for serving via FastAPI) ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# --- Configuration & Utilities ---
pyyaml>=6.0.1
python-dotenv>=1.0.0
tqdm>=4.66.0
psutil>=5.9.0
tabulate>=0.9.0

# --- Optional: GPU monitoring ---
# pynvml>=11.5.0
